# Feedback Report — 2026-04-12 Loop 2

## Diagnosed
- [x] Build: PASS
- [x] TSC: 0 errors
- [x] Target: Bug 1 — Google Maps error messages displayed on screen

## Fixed
- `components/viewport/CityScene.tsx`: Added `useGoogleMapsErrorSuppressor()` hook
  - MutationObserver watches for Google Maps error divs injected into DOM and removes them
  - Wraps `console.error` to suppress Google Maps related console errors
  - Cleanup on unmount

## Verified
- [x] TSC: 0 errors
- [x] Build passes
- Note: Full visual verification requires browser with Google Maps API key

## Remaining
- 🔴 Bug 2: Camera breaks on card selection (ECEF coordinate issue)
- 🔴 Bug 3: People dots are too large (scale mismatch)
- 🟡 errorTarget tuning
- 🟡 Minimap dots

## Next Action
- Fix Bug 2: Camera fly-to uses ECEF normal for altitude offset
