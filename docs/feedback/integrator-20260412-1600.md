# 窓2 INTEGRATOR レポート — 2026-04-12 16:00

## 対応イシュー
- #006 stores/gameStore.ts にviewMode追加

## 変更内容
- `ViewMode` type追加: `'god' | 'cross-section' | 'scene'`
- `viewMode` state + `setViewMode` action追加
- `selectedAreaId` state + `selectArea` action追加
  - `selectArea(areaId)` はareaId有無でviewModeを自動切替（cross-section / god）
- `ViewMode`型をexportして他コンポーネントから利用可能に

## ビルド確認
- TypeScriptコンパイル: ✅ 成功
- ビルド: ⚠️ `/api/submit`のpage data collection失敗（既存問題、本変更と無関係）

## Playwrightチェック
- ⏭️ スキップ（ブラウザが他の窓で使用中）

## 次のアクション
- 🔴 CRITICALの残りは窓1担当（#001-#005のコンポーネント作成）
- 窓1がコンポーネントを作ったらstoreを繋ぐ作業が発生する
- 🟡 #010 WATCH/PLAYモード切替UIのstore側準備を検討
