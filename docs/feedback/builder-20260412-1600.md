# 窓1 Builder レポート — 2026-04-12 16:00

## 完了: #002 CityMap.tsx

### 実装内容
- `components/city/CityMap.tsx` を新規作成
- 渋谷・新宿・六本木の3エリアをデフォルメSVGマップで表示
- 各エリアにカップルドットをフェーズ別カラーで配置
- パルスアニメーション（フェーズが進むほど速く脈動）
- ホバーでカップル名・フェーズ・スコア表示
- クリックで`onSelectCouple`コールバック発火
- フェーズ凡例（右下）

### 技術判断
- Framer Motionが未インストールのため、SVG `<animate>` + CSS transitionで実装
- `usePeopleStore`から300人データを取得し、`relationships`で150組をマッピング
- シードランダムでドット配置を決定論的に（リロードで位置が変わらない）

### Playwright
- 別の窓がブラウザを占有していたためスクショ未取得
- `npm run build` 成功で型安全性は確認済み

### 次のステップ
- #003 BuildingSection.tsx（断面図ビル）に着手予定
- #001（page.tsx書き換え）は全コンポーネントが揃ってから
